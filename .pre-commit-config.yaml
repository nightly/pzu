repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Pin this to a specific released version
    rev: v0.14.4
    hooks:
      # Lint + autofix
      - id: ruff-check
        args: [--fix]
      # Format (PEP 8 / Ruff style)
      - id: ruff-format